# Dependencies
node_modules/

# Build artifacts
artifacts/
cache/
typechain-types/

# Environment & secrets
.env
.env.*
!.env.example

# Beneficiary registry contains PII (phone numbers) — never commit the real file.
# Use relayer/beneficiaries.example.json as the tracked template. (A46 fix)
relayer/beneficiaries.json

# Deployment manifests (contain addresses, not secrets, but keep local)
deployment-*.json

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Coverage
coverage/
coverage.json

# Debug
debug-*.mjs
